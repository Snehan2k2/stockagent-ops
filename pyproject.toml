[project]
name = "build-stock-agent-ops"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "feast[redis]>=0.60",
    "pandas<3",
    "yfinance>=1.6.0",
]
